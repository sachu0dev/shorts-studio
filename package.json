{
  "name": "shorts-studio",
  "version": "1.0.0",
  "description": "Fully automated YouTube -> Shorts pipeline: trend research, clip selection, auto-editing, captions, thumbnails.",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,web -c auto \"tsx watch server/index.ts\" \"npm run dev --prefix web\"",
    "dev:server": "tsx watch server/index.ts",
    "start": "tsx server/index.ts",
    "build:web": "npm run build --prefix web",
    "test": "node --import tsx --test 'server/**/*.test.ts'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.57.0",
    "@google/genai": "^2.12.0",
    "busboy": "^1.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nanoid": "^5.0.7",
    "openai": "^6.48.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.5.0",
    "concurrently": "^10.0.4",
    "tsx": "^4.19.0",
    "typescript": "^5.6.2"
  }
}
